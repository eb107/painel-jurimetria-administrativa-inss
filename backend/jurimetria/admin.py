from django.contrib import admin

# Modelos não registrados aqui de propósito: o escopo do teste é uma API de
# leitura analítica, não um CRUD administrativo, então o admin do Django
# não teria uso real neste projeto.
